# Code

In classical computer theory a code is simply a subset $M \subseteq \mathbb{B}^n$ with $2^m$ elements. It's important to mention that $m$ is not necessarilly an integer. Consider for example a code with 6 values, $m$ would be $m = \log_2(6) \approx  2.58496250072$
